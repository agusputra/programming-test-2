{
  "name": "products-grid",
  "private": true,
  "version": "0.0.1",
  "homepage": ".",
  "description": "Build a grid of product items and ads",
  "main": "server/index.js",
  "scripts": {
    "start": "json-server server/index.js --routes server/routes.json --middlewares server/handle-delay.js server/handle-ads.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "client-start": "react-scripts build && serve -s build",
    "client-dev": "PORT=5000 react-scripts start",
    "client-build": "react-scripts build",
    "client-test": "TZ=UTC react-scripts test",
    "client-eject": "react-scripts eject"
  },
  "author": "",
  "license": "",
  "dependencies": {
    "axios": "^0.18.0",
    "cool-ascii-faces": "^1.3.4",
    "json-server": "^0.9.6",
    "react": "^16.6.3",
    "react-dom": "^16.6.3",
    "react-scripts": "2.1.1",
    "serve": "^10.1.1"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}